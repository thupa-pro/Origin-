# RFC-0001: Origin Protocol v1 Specification

**Status:** Stable  
**Category:** Standards Track  
**Author:** Origin Protocol Team  
**Created:** 2026-06-08  
**Updated:** 2026-06-10 (v1.2 — SHA-256 only, simplified API)

## Abstract

Origin is a cryptographic provenance protocol that binds a public key, a timestamp, and a digital artifact hash in a self-contained signed statement. The protocol defines a canonical text format for provenance statements, a deterministic signing procedure, and a verification algorithm requiring no network access or external infrastructure.

## 1. Notation

- `||` denotes byte concatenation.
- `0x` prefix denotes hexadecimal encoding.
- `b64(·)` denotes base64url encoding per RFC 4648 §5 WITH padding.
- `sha256(·)` denotes the SHA-256 hash function (FIPS 180-4).
- Ed25519 refers to the Edwards-curve Digital Signature Algorithm per RFC 8032.

## 2. Primitive Types

### 2.1 Artifact Hash

An artifact hash is a hash digest of an artifact, encoded as a lowercase hexadecimal string prefixed with an algorithm identifier.

**Format:** `<algorithm>:<lowercase hex chars>`

**Supported algorithm:**

| Algorithm | Prefix | Hex length | Binary size |
|---|---|---|---|
| SHA-256 | `sha256` | 64 | 32 bytes |

**Example:** `sha256:ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad`

### 2.2 Public Key

An Ed25519 public key is a 32-byte compressed point encoding, encoded as 44 base64url characters (with padding).

**Format:** 44 base64url characters (RFC 4648 §5, with `=` padding)  
**Binary size:** 32 bytes  
**Encoded size:** 44 ASCII characters

### 2.3 Signature

An Ed25519 signature is a 64-byte (R ‖ S) encoding, encoded as 88 base64url characters (with padding).

**Format:** 88 base64url characters (RFC 4648 §5, with `=` padding)  
**Binary size:** 64 bytes  
**Encoded size:** 88 ASCII characters

### 2.4 Timestamp

A timestamp is a non-negative decimal integer representing seconds since the UNIX epoch (1970-01-01T00:00:00Z).

**Constraint:** `0 ≤ timestamp ≤ 253402300799` (year 9999)  
**Format:** Decimal ASCII digits, no leading zeros (except the value `0` itself)

Timestamps are self-asserted by the signer. They are format-validated but NOT covered by the signature. See §9.3.

### 2.5 Statement Type

Every statement carries a `type` field. The only valid value is `provenance`. The `type` field exists for future extensibility — new statement types can be added without changing the parser.

### 2.6 Protocol Version

The protocol version is always `v1`.

## 3. Statement Format

A statement is a UTF-8 encoded text file with LF (`\n`, `0x0a`) line endings, containing colon-separated key-value pairs, one per line.

### 3.1 Without Parent

```
origin: v1
type: provenance
hash: <algorithm>:<hex>
time: <decimal timestamp>
key: <44 base64url chars>
sig: <88 base64url chars>
```

**6 lines.**

### 3.2 With Parent

```
origin: v1
type: provenance
parent: <algorithm>:<hex>
hash: <algorithm>:<hex>
time: <decimal timestamp>
key: <44 base64url chars>
sig: <88 base64url chars>
```

**7 lines.** The `parent` field links this statement to a prior statement's hash, forming a provenance chain.

### 3.3 Field Constraints

| Key | Value Constraint |
|---|---|
| `origin` | Exactly `v1` |
| `type` | Exactly `provenance` |
| `parent` | Same format as `hash` (optional, provenance only) |
| `hash` | `<algorithm>:<hex>` matching §2.1 |
| `time` | ASCII decimal integer, no leading zeros (except `0`), range [0, 253402300799] |
| `key` | Exactly 44 base64url characters (RFC 4648 §5 with padding) |
| `sig` | Exactly 88 base64url characters (RFC 4648 §5 with padding) |

### 3.4 Line Format Rules

1. Each line matches the pattern `<key>: <value>`, where `<key>` is an ASCII lowercase alphabetic string and `<value>` is a UTF-8 string with no leading or trailing whitespace.
2. The separator is exactly `: ` (colon followed by one ASCII space, `0x3a 0x20`).
3. Each line is terminated by `\n` (LF, `0x0a`).
4. The file ends with `\n` (the last line is terminated, making the file end with LF).

### 3.5 Structural Constraints

1. **Line count:** Exactly 6 lines (no parent) or exactly 7 lines (with parent).
2. **Line order:** Strict ordering per the formats above. No reordering permitted.
3. **No duplicate keys.** Each key appears exactly once.
4. **No unknown keys.** Only the defined keys are permitted.
5. **No BOM** (Byte Order Mark, `0xef 0xbb 0xbf`).
6. **No CR** (`\r`, `0x0d`) anywhere in the file.
7. **No null bytes** (`0x00`) anywhere in the file.
8. **No control characters** (code points U+0000–U+001F and U+007F) in values, except the LF line terminators.

## 4. Canonical Body

The canonical body is the input to the Ed25519 signing and verification operations.

### 4.1 Without Parent

```
origin: v1
type: provenance
hash: <hex>
key: <b64>
```

Constructed from lines 1, 2, 3, 5 (origin, type, hash, key) of the statement file, joined by `\n`, with NO trailing newline.

### 4.2 With Parent

```
origin: v1
type: provenance
parent: <hex>
hash: <hex>
key: <b64>
```

Constructed from lines 1, 2, 3, 4, 6 (origin, type, parent, hash, key) of the statement file, joined by `\n`, with NO trailing newline.

## 5. Signing Procedure

### 5.1 Inputs

- Ed25519 secret key `sk` (32 bytes)
- Artifact bytes `data` (arbitrary length)
- Timestamp `ts` (non-negative integer)
- Optional parent hash `parent` (if chaining)

### 5.2 Algorithm

```
1. hash = sha256(data)
2. hex_hash = lowercase_hex(hash)
3. Encoded fields:
   - origin_line = "origin: v1"
   - type_line = "type: provenance"
   - parent_line = "parent: <parent>"       // if parent present
   - hash_line = "hash: sha256:" || hex_hash
   - key_line = "key: " || b64(pubkey(sk))
4. canonical = join(lines, "\n")            // no trailing newline
5. sig = ed25519_sign(sk, canonical)
6. Append "sig: " || b64(sig) || "\n"
7. return statement
```

### 5.3 Output

The output is a UTF-8 encoded byte sequence (the statement file content).

## 6. Verification Procedure

### 6.1 Inputs

- Statement bytes (the complete statement file content)
- Artifact bytes `data`

### 6.2 Algorithm

```
1. Parse statement into 6 or 7 fields (§3).
2. Validate all field constraints (§3.3, §3.5).
3. Verify type == "provenance".
4. Reconstruct canonical body (§4.1 or §4.2).
5. Decode sig from base64url to 64 bytes.
6. Decode key from base64url to 32 bytes.
7. actual_hash = <algorithm>(data) per hash prefix.
8. If actual_hash ≠ parsed hash: FAILED (hash mismatch).
9. If ed25519_verify(key, canonical, sig) fails: FAILED (signature mismatch).
10. Return VERIFIED.
```

### 6.3 Verification Results

| Result | Meaning |
|---|---|
| `VERIFIED` | The statement is cryptographically valid for the given artifact |
| `FAILED` | The statement is invalid — see error message |

The verification procedure MUST NOT return any third state (e.g., "unknown", "untrusted", "expired").

## 7. Key Generation

Ed25519 key pairs are generated per RFC 8032 §5.1.5:

1. Generate 32 cryptographically secure random bytes as the seed.
2. Hash the seed with SHA-512.
3. Prune the first 32 bytes: clear bits 0–3, clear bit 254, set bit 255.
4. The secret key is the original 32-byte seed.
5. The public key is the encoded scalar multiplication of the pruned hash with the basepoint.

## 8. Audit Output

The audit function parses a statement and displays its fields in human-readable form. It does NOT perform cryptographic verification. It is a diagnostic tool.

**Output format:**

```
Statement Audit
├─ Origin:  v1
├─ Parent:  sha256:<hex>               (if present)
├─ Hash:    sha256:<hex> (SHA-256)
├─ Time:    2024-06-07T16:00:00Z (<decimal>) — advisory
├─ Key:     <base64url>
└─ Sig:     <base64url>
```

## 9. Security Considerations

### 9.1 Cryptographic Assumptions

The protocol is secure under the following standard cryptographic assumptions:

- Ed25519 provides existential unforgeability under chosen-message attack (EUF-CMA).
- SHA-256 provides collision resistance (≤ 2^128 operations).
- The 256-bit Ed25519 scalar field provides 128-bit security against discrete log attacks.

### 9.2 Hash Agility

The protocol supports SHA-256. SHA-256 is FIPS 180-4 compliant. This is the only hash function used by Origin — it provides 128-bit collision resistance, which matches Ed25519's 128-bit security level.

### 9.3 Timestamp Advisory

Timestamps are self-asserted by the signer. The verifier only checks that the timestamp is a well-formed non-negative integer within an acceptable range. There is no mechanism to verify that the timestamp reflects "real" time.

The timestamp is NOT included in the canonical body. Changing the timestamp does NOT invalidate the signature. This is intentional — signing a value you cannot verify is dishonest. The timestamp is a claim, not a proof.

### 9.4 Side-Channel Resistance

The reference implementation uses `ed25519-dalek`, which implements constant-time point operations. However, the environment (operating system, hardware) may introduce timing variability outside the library's control.

### 9.5 TOCTOU

See threat model (THREAT_MODEL.md). Verification is a snapshot operation of caller-provided byte buffers. File-system-level verification is a convenience wrapper with documented race conditions.

### 9.6 Key Compromise

A compromised signing key cannot be revoked at the protocol level. Stop trusting statements signed after the compromise date. Key distribution and revocation are outside the protocol scope.

## 10. CLI Reference

### `origin hash <path>`
Compute the SHA-256 hash of a file.

### `origin keygen [--output <dir>]`
Generate a new Ed25519 key pair. Secret key permissions set to 0o600 on Unix.

### `origin sign <path> [--key <file>] [--time <ts>] [--parent <path>]`
Sign an artifact, producing a `.origin` statement file. Uses current time by default.
Secret key from `--key <file>`, `ORIGIN_KEY` env var, or `--key -` (stdin).

### `origin verify <statement> <artifact>`
Verify a provenance statement against an artifact. Returns VERIFIED or FAILED.

### `origin audit <statement>`
Display a human-readable audit of a statement.

## 11. Test Vectors

### 11.1 Hash Computation

```
Input:    (empty)
SHA-256:  e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

Input:    "abc"
SHA-256:  ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad
```

### 11.2 Key Generation

```
Seed:     9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60
Public:   d75a980182b10ab7d54bfed3c964073a0ee172f3daa62325af021a68f707511a
```

### 11.3 Provenance Statement (empty artifact, timestamp 0)

```
Seed:     9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60
Artifact: (empty)
Time:     0

Statement:
origin: v1
type: provenance
hash: sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
time: 0
key: 11qYAYKxCrfVS_7TyWQHOg7hcvPapiMlrwIaaPcHURo=
sig: Kh6TgcsLBCWrWJRP0xTidWPAB59RSu-6ON90LIxVsNYhI5tTR2AqlavX8uL_VApO8SsI5opQt1r394ITq4czBg==
```

### 11.4 Parent Chain (two links, different keys)

Parent uses seed from §11.2, child uses a different seed. The child's `parent` field references the parent's artifact hash. Verification succeeds when `origin verify child.origin child.tar.gz --parent parent.origin /dev/null --trusted-key child-public.key` is run.

```
Parent seed: 9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60
Child seed:  2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a2a
             (42 repeated 32 times)
Parent artifact: (empty)
Child artifact:  "abc"
Parent time:     0
Child time:      1

Parent statement:
origin: v1
type: provenance
hash: sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
time: 0
key: 11qYAYKxCrfVS_7TyWQHOg7hcvPapiMlrwIaaPcHURo=
sig: Kh6TgcsLBCWrWJRP0xTidWPAB59RSu-6ON90LIxVsNYhI5tTR2AqlavX8uL_VApO8SsI5opQt1r394ITq4czBg==

Child statement:
origin: v1
type: provenance
parent: sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
hash: sha256:ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad
time: 1
key: GX9rI-FshTLGq8g4-s1ep4m-DHaykgM0A5v6iz02jWE=
sig: nnrbbaF_pBtex4zKX1OohGdk8nvaFOnwiWMFcAqXLxO2X-ufJdQXeAM22iQxbtVgnAMJnZpDYgDu_HDEQlTHDA==
```

## 12. References

- RFC 8032: Edwards-Curve Digital Signature Algorithm (EdDSA)
- RFC 4648: The Base16, Base32, and Base64 Data Encodings
- FIPS 180-4: Secure Hash Standard (SHA-256)
- RFC 2119: Key words for use in RFCs to Indicate Requirement Levels
