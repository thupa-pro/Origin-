[package]
name = "origin-wasm-example"
version = "1.2.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
origin-core = { path = "../../origin-core", version = "1.2.0" }
wasm-bindgen = "0.2"
